# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# The core gem, from this repo, while hibiki_rails incubates here.
gem "hibiki", path: ".."

group :development do
  gem "rake", "~> 13.0"
  gem "rspec", "~> 3.0"
  gem "rspec-rails", "~> 8.0"
  gem "rubocop", "~> 1.21"
end
